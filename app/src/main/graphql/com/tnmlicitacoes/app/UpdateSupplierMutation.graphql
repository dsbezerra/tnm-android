mutation UpdateSupplierMutation($supplier: SupplierInput!) {
    updateSupplier(supplier: $supplier) {
      id
      cityNum
      segNum
      activated
      phone
      defaultCard
      email
    }
}