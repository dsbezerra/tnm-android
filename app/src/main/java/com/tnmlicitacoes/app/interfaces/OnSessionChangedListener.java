package com.tnmlicitacoes.app.interfaces;

public interface OnSessionChangedListener {
    /**
     * Called when a new device has been logged with
     * the same phone number
     */
    void onDeviceChanged();
}
