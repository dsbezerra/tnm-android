<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingStart="@dimen/spacing_normal"
    android:paddingEnd="@dimen/spacing_normal">

    <TextView
        android:id="@+id/email_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="E-mail para contato"
        android:textSize="18sp"
        android:textColor="@color/colorPrimary"
        android:fontFamily="sans-serif-light"
        android:layout_marginBottom="@dimen/spacing_large"
        />

    <android.support.design.widget.TextInputLayout
        android:id="@+id/email_layout"
        app:hintTextAppearance="@style/Widget.TNM.TextInput"
        android:textColorHint="@color/colorLightGrey"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <android.support.design.widget.TextInputEditText
            android:id="@+id/email_value"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textEmailAddress"
            android:hint="nome@exemplo.com"
            android:maxLength="50"
            android:maxLines="1"
            android:drawableEnd="@drawable/ic_clear_select_search_field"
            />

    </android.support.design.widget.TextInputLayout>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/input_email_info_message"
        android:textColor="#808080"
        android:padding="@dimen/spacing_normal"
        android:background="@drawable/bg_input_email_info"
        android:layout_marginTop="@dimen/spacing_normal"
        android:layout_marginBottom="@dimen/spacing_normal"
        android:drawableStart="@drawable/ic_info_outline"
        android:drawablePadding="@dimen/spacing_normal"
        android:drawableTint="#808080"
        />

</LinearLayout>